# Example

This package is primarily a CLI. The example below shows how to call the CLI
programmatically from Dart:

```bash
dart run example/main.dart
```

For normal CLI usage:

```bash
flutter_app_template_cli create my_workspace --template monorepo
```
